{
  "name": "bitmapit-preview",
  "version": "1.0.0",
  "description": "Bitmap font generator preview",
  "private": true,
  "scripts": {
    "dev": "serve .",
    "start": "serve .",
    "build": "mkdir -p public && cp -r index.html node_modules public/",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "bitmapit": "^1.0.3"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
